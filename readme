Eval/AvgLines
意思是：评估时，AI 平均每局能消掉多少行。
这是最核心的“AI 会不会玩”的指标。
数值越高，通常越好。
如果一直接近 0，说明 AI 还不会稳定清行。
如果从 1、2、5、10 这样慢慢往上走，就是明显在进步。

Eval/ClearRate
意思是：评估时，有多少比例的对局“至少成功消过行”。
比如：
1.0 就是 100% 的评估局都消过行
0.5 就是只有一半的局消过行
这个指标很适合看“AI 有没有开窍”。
因为有时候 AvgLines 还不高，但 ClearRate 已经开始涨了，说明它至少会一点了。

Eval/AvgReward
意思是：评估时，AI 平均每局拿到的总奖励。
它反映的是整体策略质量，不只是清行。
因为奖励里还包含：
活得久不久
有没有堆高
有没有制造空洞
有没有被打爆
所以这个值高，一般是好事。
但它不如 AvgLines 那么直观。

Eval/AvgSteps
意思是：评估时，AI 平均每局撑了多少步。
你可以理解成“平均活了多久”。
如果这个值上升，通常说明 AI 的生存能力变强了。
但如果它只是活得久，却不清行，那也不算真的变强。

Train/Reward
意思是：训练过程中某一局的总奖励。
波动会很大。
只能参考，不适合单独拿来判断模型好坏。

Train/Loss
意思是：神经网络这次更新时，预测值和目标值差了多少。
它是“学得稳不稳”的指标，不是“会不会玩”的指标。
低一点通常好，但不是越低越神。
重点看它别爆炸、别一直乱跳。

Train/Epsilon
意思是：当前随机探索的程度。
越高表示 AI 越爱乱试。
越低表示 AI 越依赖自己当前学到的策略。
训练前期高是正常的，后期应该逐渐降下来。

Train/MALines
意思是：最近一段训练局数里，平均每局消掉多少行。
MA 就是移动平均。
它比单局 Train/Lines 稳定很多，更适合看趋势。

Train/MAReward
意思，是：最近一段训练局数里的平均奖励。
也是为了看趋势，不看单局波动。

如果你只想抓最重要的两个中文理解：

Eval/AvgLines
AI 平均每局能消几行，这是“会不会玩”的最重要指标。

Eval/ClearRate
AI 有多少局至少能成功消行，这是“有没有开窍”的最敏感指标。

如果你愿意，我下一条可以直接给你一个“看到这些数值时该怎么判断”的中文对照表。


cd /home/lidio/Tetris_Python
source .venv/bin/activate
python3 train_nextstate.py --episodes 2000 --device cuda

python -m tensorboard.main --logdir models/next_state/tensorboard
